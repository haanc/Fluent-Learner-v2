# Implementation Plan: Epic 3 - The Immersive Player

本计划涵盖 Phase 1 的核心功能：实现视频与字幕的深度交互。

## 🎯 目标
*   构建高性能的字幕侧边栏。
*   实现视频播放头与字幕高亮的双向同步。
*   实现点击单词弹出查词卡片（UI 层面）。

## 🛠️ 拟进行的更改

### Frontend (React + Tailwind)

#### [NEW] [SubtitleSidebar.tsx](file:///c:/Users/hancao/.gemini/antigravity/scratch/language-learner/fluent-learner-v2/src/components/SubtitleSidebar.tsx)
*   创建字幕列表组件。
*   使用 `virtualized list` 处理长视频（数千行字幕）的性能。
*   逻辑：接收 [segments](file:///c:/Users/hancao/.gemini/antigravity/scratch/language-learner/fluent-learner-v2/backend/main.py#42-55) 数组和 `currentTime`，渲染并滚动到当前行。

#### [MODIFY] [App.tsx](file:///c:/Users/hancao/.gemini/antigravity/scratch/language-learner/fluent-learner-v2/src/App.tsx)
*   集成 `SubtitleSidebar`。
*   添加 `onTimeUpdate` 监听器，将视频时间传递给子组件。
*   添加 `onSeek` 逻辑，允许点击字幕跳转视频。

#### [NEW] [WordPopover.tsx](file:///c:/Users/hancao/.gemini/antigravity/scratch/language-learner/fluent-learner-v2/src/components/WordPopover.tsx)
*   使用 `@radix-ui/react-popover` 或 `Floating UI`。
*   实现点击字幕中单词后在正上方弹出的交互效果。

## ✅ 验证计划

### 自动化/性能测试
*   **长字幕渲染**: 模拟 1000 条字幕，验证滚动不掉帧。
*   **同步精度**: 播放视频，验证字幕高亮切换的延迟小于 100ms。

### 手动验证
1.  启动应用，加载一个带有 Mock 字幕的数据。
2.  播放视频，观察侧边栏是否自动滚动并高亮当前句。
3.  点击任一字幕行，验证视频是否立即跳转到该时间点。
4.  点击字幕中的单词，验证气泡弹出位置正确。
