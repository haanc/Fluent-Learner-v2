# Implementation Plan: Epic 1 - Scaffold & Tech Spike (Retrospective)

本计划记录了项目启动阶段的技术实现细节。

## 🎯 目标
*   搭建 Electron + Vite + React (TypeScript) 开发环境。
*   集成 Python 子进程管理，实现前后端同步。
*   解决浏览器文件安全限制，实现本地媒体播放。
*   验证视频图层交互可行性。

## 🛠️ 已实施的更改

### Infrastructure & Tooling
*   初始化 `fluent-learner-v2` 目录。
*   安装 `electron-vite` 脚手架。

### Backend (Python)
#### [NEW] [main.py](file:///c:/Users/hancao/.gemini/antigravity/scratch/language-learner/fluent-learner-v2/backend/main.py)
*   建立 FastAPI 基础服务。
*   提供 `/health` 健康检查接口。

### Frontend (Electron Main Process)
#### [MODIFY] [main.ts](file:///c:/Users/hancao/.gemini/antigravity/scratch/language-learner/fluent-learner-v2/electron/main.ts)
*   **子进程管理**: 使用 `child_process.spawn` 启动 `backend/venv/python.exe`。
*   **协议注册**: 使用 `protocol.handle` 注册 `local-video://`，绕过 CORS 播放本地文件。
*   **IPC 通信**: 注册 `open-file-dialog` 用于调用系统文件选择器。

### Frontend (React Renderer)
#### [MODIFY] [App.tsx](file:///c:/Users/hancao/.gemini/antigravity/scratch/language-learner/fluent-learner-v2/src/App.tsx)
*   实现基础视频播放 UI。
*   实现悬浮在视频上方的交互按钮 (Z-Index 布局)。

## ✅ 验证结果
*   **Electron 启动正常**: 能够显示白色窗口并渲染 React 组件。
*   **Python 自动启动**: Electron 启动后，终端显示 Uvicorn 运行日志。
*   **视频播放成功**: 能够通过文件选择器选取本地 MP4 并播放。
*   **交互可行**: 视频上方按钮点击响应 Alert，证明图层布局正确。
